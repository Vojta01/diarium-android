# Build outputs
build/
app/build/
*.apk
*.aab

# Local machine configuration (SDK paths, secrets) — never commit
local.properties

# Firebase credentials
# Client config (google-services.json) IS committed — it contains only client
# identifiers already shipped inside the APK. NEVER commit service accounts:
*.json.serviceaccount
*.p12

# IDE
.idea/
*.iml
.gradle/
.kotlin/

# OS
.DS_Store