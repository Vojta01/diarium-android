# Build outputs
build/
app/build/
*.apk
*.aab

# Local machine configuration (SDK paths, secrets) — never commit
local.properties

# Release signing key material — never commit
keystore.properties
*.jks
*.keystore

# Firebase credentials — user-provided, never commit
google-services.json
*.json.serviceaccount
*.p12

# IDE
.idea/
*.iml
.gradle/
.kotlin/

# OS
.DS_Store